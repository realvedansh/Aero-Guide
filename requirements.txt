flask
python-dotenv
groq
openai
requests
ddgs
gunicorn
Flask-Limiter==3.9.2
Flask-SQLAlchemy==3.1.1
# Production server
gunicorn==23.0.0
gevent==24.11.1

# Optional (recommended for 5k+ concurrent users): shared cache /
# rate-limit storage across multiple gunicorn workers/machines
redis==5.2.0

# Optional (recommended for production instead of SQLite)
psycopg2-binary==2.9.10
